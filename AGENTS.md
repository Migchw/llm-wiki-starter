# Agent Contract

> Canonical project-agent configuration lives in [`.agents/AGENTS.md`](.agents/AGENTS.md). This root file exists only because many coding agents (Cursor, etc.) discover `AGENTS.md` from the repository root — read the linked file, not this one, for the actual routing table, agent roster, and rules.
